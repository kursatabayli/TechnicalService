<MudAppBar Elevation="1" Class="px-4" Dense="true">
    <!-- Sol Taraf -->
    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="3" >
        <AuthorizeView>
            <MudIconButton Icon="@Icons.Material.Filled.Menu"
                           Color="Color.Inherit"
                           Edge="Edge.Start"
                           Class="ml-n1"
                           OnClick="@((e) => DrawerToggle())" />
        </AuthorizeView>

        <MudLink Href="/admin" Underline="Underline.None" Typo="Typo.h6" Class="font-weight-bold text-uppercase" Color="Color.Primary">
            Hoş Geldiniz
        </MudLink>
    </MudStack>
    <MudSpacer />
    <!-- Sağ Taraf -->
    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="3">
        <MudToggleIconButton ToggledChanged="@((e) => DarkModeToggle())"
                             Class="ml-2"
                             Icon="@(UIStateOption.isDarkMode ? Icons.Material.Filled.DarkMode : Icons.Material.Filled.WbSunny)"
                             Color="Color.Inherit"
                             Title="@(UIStateOption.isDarkMode ? "Karanlık Mod" : "Aydınlık Mod")" />
    </MudStack>

</MudAppBar>